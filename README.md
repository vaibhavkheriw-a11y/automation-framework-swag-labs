# automation-framework-swag-labs
Run the appropriate .bat file (such as allure-smoke, allure-sanity, or allure-regression) to execute the required test suite. Once the tests have completed, run allure serve allure_report to view the Allure report.
